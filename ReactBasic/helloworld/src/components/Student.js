import React from 'react'

export const Student = ({age}) => {
    console.log(age)
    return (
        <div>
            <button onClick={() => {
                
            }}>clicsdfk</button>
        </div>
    )
}
